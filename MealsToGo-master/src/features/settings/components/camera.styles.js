import styled from "styled-components/native";
import { CameraView, CameraType, useCameraPermissions } from 'expo-camera';

export const ProfileCamera = styled(CameraView)`
    width:100%;
    height:100%;
`;